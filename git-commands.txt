📦 COMMANDES GIT À EXÉCUTER DIRECTEMENT:

# 1. Ajouter tous les fichiers
git add -A

# 2. Faire un commit avec tous les changements
git commit -m "VPS deployment fixes - Stripe hardcoded, Qt error fixed, build scripts added"

# 3. Pousser vers votre repository
git push origin main

# Ou si votre branche est 'master':
git push origin master

# Si vous n'avez pas encore configuré le remote:
git remote add origin https://github.com/VOTRE_USERNAME/VOTRE_REPO.git
git push -u origin main

✅ Tous les fichiers seront poussés!